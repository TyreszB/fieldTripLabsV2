type Query {
    lisrtUsers(limit: Int, nextTokent: string): UserConnection
}

type User {
    id: ID!
    userName: String!
    password: String!
    firstName: String!
    lastName: String!
    email: String!
    iamge: AWSImage
    createdAt: String!
    updatedAt: String!
}

type UserConnection {
    items: [User!]
    nextToken: String
}